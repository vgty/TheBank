<%if @user.counselor.name%>
  <h3>Your counselor is <%=@user.counselor.name %></h3>
<%else%>
  <h3>You don't have counselor yet</h3>
<%end%>
<%=render 'your_accounts'%>
<%=render 'your_saving_accounts'%>

<%= link_to 'Edit', edit_user_path(@user), class: "btn btn-dark btn-sm rounded-0" %> 
<%= link_to 'Back', root_path, class: "btn btn-dark btn-sm rounded-0" %>